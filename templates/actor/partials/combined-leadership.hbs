{{!-- 5-man: Combined Command Allocation — Presence roll drives Resolve, Efficiency & Expedience --}}
<div class="sheet-list">
    <div class="list-header row-content imsc-leadership-header">
        <div class="list-name">{{localize "IMSC.Captain.CommandAllocation"}}</div>
        <a class="imsc-full-reset-link" data-action="rollInitiative"
           data-tooltip="{{localize 'IMSC.Captain.RollInitiativeBtn'}}">
            <i class="fa-solid fa-dice-d20"></i>
        </a>
    </div>
    <div class="list-content">
        <div class="imsc-helm-stats">

            {{!-- Presence (Leadership) roll --}}
            <div class="imsc-helm-stat" data-tooltip="{{captainCtx.slTooltip}}">
                <span class="imsc-helm-stat-label">{{captainCtx.slLabel}}</span>
                {{#if captainCtx.leadershipRolled}}
                    <span class="imsc-helm-stat-value imsc-sl-badge">{{captainCtx.remainingLeadershipSL}}</span>
                {{else}}
                    <button class="imsc-roll-piloting-btn" data-action="rollLeadershipSL">
                        <i class="fa-solid fa-dice"></i> {{captainCtx.rollLabel}}
                    </button>
                {{/if}}
            </div>

            {{!-- Resolve --}}
            <div class="imsc-helm-stat" data-tooltip="{{localize 'IMSC.Captain.ResolveTooltip'}}">
                <span class="imsc-helm-stat-label">{{localize "IMSC.Captain.Resolve"}}</span>
                <div class="imsc-alloc-row">
                    <button class="imsc-alloc-btn" data-action="allocLeadershipSL" data-stat="resolve" data-delta="-1"
                            {{#unless captainCtx.leadershipRolled}}disabled{{/unless}}
                            {{#if captainCtx.hasPlayedCards}}disabled{{/if}}
                            {{#if ordnanceCtx.actionUsed}}disabled{{/if}}
                            {{#unless captainCtx.allocResolve}}disabled{{/unless}}>
                        &minus;
                    </button>
                    <span class="imsc-helm-stat-value">{{captainCtx.allocResolve}}</span>
                    <button class="imsc-alloc-btn" data-action="allocLeadershipSL" data-stat="resolve" data-delta="1"
                            {{#unless captainCtx.leadershipRolled}}disabled{{/unless}}
                            {{#if captainCtx.hasPlayedCards}}disabled{{/if}}
                            {{#if ordnanceCtx.actionUsed}}disabled{{/if}}
                            {{#if (gte (add (add captainCtx.allocResolve ordnanceCtx.allocEfficiency) ordnanceCtx.allocExpedience) captainCtx.leadershipSL)}}disabled{{/if}}>
                        +
                    </button>
                </div>
            </div>

            {{!-- Efficiency --}}
            <div class="imsc-helm-stat" data-tooltip="{{localize 'IMSC.Ordnance.EfficiencyTooltip'}}">
                <span class="imsc-helm-stat-label">{{localize "IMSC.Ordnance.Efficiency"}}</span>
                <div class="imsc-alloc-row">
                    <button class="imsc-alloc-btn" data-action="allocOrdnanceSL" data-stat="efficiency" data-delta="-1"
                            {{#unless captainCtx.leadershipRolled}}disabled{{/unless}}
                            {{#if captainCtx.hasPlayedCards}}disabled{{/if}}
                            {{#if ordnanceCtx.actionUsed}}disabled{{/if}}
                            {{#unless ordnanceCtx.allocEfficiency}}disabled{{/unless}}>
                        &minus;
                    </button>
                    <span class="imsc-helm-stat-value">{{ordnanceCtx.allocEfficiency}}</span>
                    <button class="imsc-alloc-btn" data-action="allocOrdnanceSL" data-stat="efficiency" data-delta="1"
                            {{#unless captainCtx.leadershipRolled}}disabled{{/unless}}
                            {{#if captainCtx.hasPlayedCards}}disabled{{/if}}
                            {{#if ordnanceCtx.actionUsed}}disabled{{/if}}
                            {{#if (gte (add (add captainCtx.allocResolve ordnanceCtx.allocEfficiency) ordnanceCtx.allocExpedience) captainCtx.leadershipSL)}}disabled{{/if}}>
                        +
                    </button>
                </div>
            </div>

            {{!-- Expedience --}}
            <div class="imsc-helm-stat" data-tooltip="{{localize 'IMSC.Ordnance.ExpedienceTooltip'}}">
                <span class="imsc-helm-stat-label">{{localize "IMSC.Ordnance.Expedience"}}</span>
                <div class="imsc-alloc-row">
                    <button class="imsc-alloc-btn" data-action="allocOrdnanceSL" data-stat="expedience" data-delta="-1"
                            {{#unless captainCtx.leadershipRolled}}disabled{{/unless}}
                            {{#if captainCtx.hasPlayedCards}}disabled{{/if}}
                            {{#if ordnanceCtx.actionUsed}}disabled{{/if}}
                            {{#unless ordnanceCtx.allocExpedience}}disabled{{/unless}}>
                        &minus;
                    </button>
                    <span class="imsc-helm-stat-value">{{ordnanceCtx.allocExpedience}}</span>
                    <button class="imsc-alloc-btn" data-action="allocOrdnanceSL" data-stat="expedience" data-delta="1"
                            {{#unless captainCtx.leadershipRolled}}disabled{{/unless}}
                            {{#if captainCtx.hasPlayedCards}}disabled{{/if}}
                            {{#if ordnanceCtx.actionUsed}}disabled{{/if}}
                            {{#if (gte (add (add captainCtx.allocResolve ordnanceCtx.allocEfficiency) ordnanceCtx.allocExpedience) captainCtx.leadershipSL)}}disabled{{/if}}>
                        +
                    </button>
                </div>
            </div>

        </div>
    </div>
</div>
