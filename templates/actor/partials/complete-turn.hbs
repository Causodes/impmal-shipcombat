{{!-- Toggle Action Done – toggles turnDone[roleId] on the ship actor --}}
{{!-- Expects: tabRoleId (string), and turnDone from the role's context --}}
<div class="imsc-complete-turn-row">
    {{#if turnDone}}
    <button type="button" class="imsc-mark-done-btn done" data-action="markDone" data-role-id="{{tabRoleId}}">
        <i class="fa-solid fa-rotate-left"></i>
        {{localize "IMSC.Action.UndoAction"}}
    </button>
    {{else}}
    <button type="button" class="imsc-mark-done-btn" data-action="markDone" data-role-id="{{tabRoleId}}">
        <i class="fa-solid fa-check"></i>
        {{localize "IMSC.Action.MarkDone"}}
    </button>
    {{/if}}
</div>
