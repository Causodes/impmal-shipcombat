{{!-- Read-only resource bar shared by torpedo and strike-craft headers.
     Parameters: label, fillClass, pct, value, max --}}
<div class="imsc-torpedo-bar">
  <label>{{label}}</label>
  <div class="imsc-torpedo-bar-track">
    <div class="imsc-torpedo-bar-fill {{fillClass}}" style="width: {{pct}}%"></div>
    <span class="imsc-torpedo-bar-text">{{value}} / {{max}}</span>
  </div>
</div>
