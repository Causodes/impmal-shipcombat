<section class="tab {{tab.cssClass}}" data-group="primary" data-tab="{{tab.id}}">

    {{!-- ═══ Torpedo Loadout ═══ --}}
    <div class="sheet-list">
        <div class="list-header row-content">
            <div class="list-name">{{localize "IMSC.NpcShip.TorpedoTemplates"}}</div>
        </div>
        <div class="list-content" data-ordnance-drop="1" data-ordnance-slot="torpedo">
            {{#if torpedoTemplates.length}}
            <div class="imsc-ov-ordnance-box">
                {{#each torpedoTemplates}}
                <div class="imsc-ov-comp-row" data-template-id="{{id}}" data-ordnance-slot="torpedo">
                    <a class="imsc-ov-comp-chip imsc-ord-chip" data-action="npcOpenOrdTemplate"
                       data-tooltip="{{localize 'IMSC.NpcShip.OpenSheet'}}">
                        {{#if img}}<img src="{{img}}">{{/if}}<span>{{name}}</span>
                    </a>
                    <a class="imsc-ov-unassign" data-action="npcRemoveOrdTemplate"
                       data-tooltip="{{localize 'IMSC.NpcShip.RemoveTemplate'}}">
                        <i class="fa-solid fa-xmark"></i>
                    </a>
                </div>
                {{/each}}
            </div>
            {{else}}
            <div class="imsc-ord-drop-hint">
                <i class="fa-solid fa-circle-down"></i> {{localize "IMSC.NpcShip.DropTorpedoHint"}}
            </div>
            {{/if}}
        </div>
        {{#if torpedoTemplates.length}}
        <div class="imsc-npc-launch-row">
            <span class="imsc-npc-launch-label">{{localize "IMSC.NpcShip.TorpedoCount"}}</span>
            <input type="number" class="imsc-npc-launch-count"
                   data-config="torpedoCount"
                   data-template-id="{{torpedoTemplates.[0].id}}"
                   value="{{torpedoTemplates.[0].torpedoCount}}"
                   min="1" max="20">
            <button class="imsc-battery-fire-btn" data-action="npcLaunchTorpedo">
                <i class="fa-solid fa-rocket"></i> {{localize "IMSC.NpcShip.LaunchTorpedo"}}
            </button>
        </div>
        {{/if}}
    </div>

    {{!-- ═══ Strike Craft Loadout ═══ --}}
    <div class="sheet-list">
        <div class="list-header row-content">
            <div class="list-name">{{localize "IMSC.NpcShip.StrikeCraftTemplates"}}</div>
        </div>
        <div class="list-content" data-ordnance-drop="1" data-ordnance-slot="strikeCraft">
            {{#if craftTemplates.length}}
            <div class="imsc-ov-ordnance-box">
                {{#each craftTemplates}}
                <div class="imsc-ov-comp-row" data-template-id="{{id}}" data-ordnance-slot="strikeCraft">
                    <a class="imsc-ov-comp-chip imsc-ord-chip" data-action="npcOpenOrdTemplate"
                       data-tooltip="{{localize 'IMSC.NpcShip.OpenSheet'}}">
                        {{#if img}}<img src="{{img}}">{{/if}}<span>{{name}}</span>
                    </a>
                    <a class="imsc-ov-unassign" data-action="npcRemoveOrdTemplate"
                       data-tooltip="{{localize 'IMSC.NpcShip.RemoveTemplate'}}">
                        <i class="fa-solid fa-xmark"></i>
                    </a>
                </div>
                {{/each}}
            </div>
            {{else}}
            <div class="imsc-ord-drop-hint">
                <i class="fa-solid fa-circle-down"></i> {{localize "IMSC.NpcShip.DropCraftHint"}}
            </div>
            {{/if}}
        </div>
        {{#if craftTemplates.length}}
        <div class="imsc-npc-launch-row">
            <span class="imsc-npc-launch-label">{{localize "IMSC.NpcShip.SquadronSize"}}</span>
            <input type="number" class="imsc-npc-launch-count"
                   data-config="squadronSize"
                   data-template-id="{{craftTemplates.[0].id}}"
                   value="{{craftTemplates.[0].squadronSize}}"
                   min="1" max="20">
            <button class="imsc-battery-fire-btn" data-action="npcLaunchStrikeCraft">
                <i class="fa-solid fa-jet-fighter"></i> {{localize "IMSC.NpcShip.LaunchStrikeCraft"}}
            </button>
        </div>
        {{/if}}
    </div>

    {{!-- ═══ Deployed Ordnance ═══ --}}
    <div class="sheet-list imsc-deployed-ordnance">
        <div class="list-header row-content">
            <div class="list-name">{{localize "IMSC.NpcShip.DeployedOrdnance"}} ({{deployedCount}})</div>
        </div>
        <div class="list-content">
            {{#unless deployedCount}}
            <div class="imsc-deployed-empty">
                {{localize "IMSC.NpcShip.NoDeployedOrdnance"}}
            </div>
            {{/unless}}

            {{!-- Torpedoes --}}
            {{#each deployedTorpedoes}}
            <div class="imsc-deployed-row imsc-deployed--torpedo {{#if turnComplete}}imsc-deployed--done{{/if}}"
                 data-token-id="{{tokenId}}">
                <img src="{{img}}" class="imsc-deployed-icon" />
                <span class="imsc-deployed-name">{{name}}</span>
                <span class="imsc-deployed-type"><i class="fa-solid fa-rocket"></i></span>
                <div class="imsc-deployed-hull">
                    <span class="imsc-hull-fraction">{{hull.value}}/{{hull.max}}</span>
                    <span class="imsc-hull-label">hp</span>
                </div>
                <div class="imsc-deployed-controls">
                    <button data-action="panToOrdnance" data-token-id="{{tokenId}}"
                            data-tooltip="{{localize 'IMSC.Ordnance.PanTo'}}">
                        <i class="fa-solid fa-eye"></i>
                    </button>
                </div>
            </div>
            {{/each}}

            {{!-- Strike Craft --}}
            {{#each deployedCraft}}
            <div class="imsc-deployed-row imsc-deployed--craft {{#if turnComplete}}imsc-deployed--done{{/if}} {{#if rtb}}imsc-deployed--rtb{{/if}}"
                 data-token-id="{{tokenId}}">
                <img src="{{img}}" class="imsc-deployed-icon" />
                <span class="imsc-deployed-name">{{name}}</span>
                <span class="imsc-deployed-type">
                    <i class="fa-solid fa-jet-fighter-up"></i>
                    {{#if rtb}}<i class="fa-solid fa-rotate-left imsc-rtb-icon"></i>{{/if}}
                </span>
                <div class="imsc-deployed-hull">
                    <span class="imsc-hull-fraction">{{hull.value}}/{{hull.max}}</span>
                    <span class="imsc-hull-label">losses</span>
                </div>
                <div class="imsc-deployed-controls">
                    <button data-action="panToOrdnance" data-token-id="{{tokenId}}"
                            data-tooltip="{{localize 'IMSC.Ordnance.PanTo'}}">
                        <i class="fa-solid fa-eye"></i>
                    </button>
                </div>
            </div>
            {{/each}}
        </div>
        {{#if deployedCraft.length}}
        <div class="imsc-battery-actions">
            <button class="imsc-battery-fire-btn" data-action="npcRTB"
                    data-tooltip="{{localize 'IMSC.Ordnance.RecallCraftRange'}}">
                <i class="fa-solid fa-plane-arrival"></i> {{localize "IMSC.NpcShip.RTBCraft"}}
            </button>
        </div>
        {{/if}}
    </div>

</section>
