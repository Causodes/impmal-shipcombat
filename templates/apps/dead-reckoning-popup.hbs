{{!-- dead-reckoning-popup.hbs  -  Drag cards to set the draw order --}}
<div class="imsc-dr-popup">

    <p class="imsc-dr-instruction">
        <i class="fa-solid fa-grip-lines"></i>
        Drag cards to set the draw order. Position 1 will be drawn next.
    </p>

    <ol class="imsc-dr-list">
        {{#each cards}}
        <li class="imsc-dr-card" draggable="true" data-card-id="{{id}}">
            <span class="imsc-dr-pos">{{pos}}</span>
            <span class="imsc-dr-label">{{label}}</span>
            <i class="fa-solid fa-grip-lines imsc-dr-grip"></i>
        </li>
        {{/each}}
    </ol>

    {{#if tailCount}}
    <p class="imsc-dr-tail">
        <i class="fa-solid fa-layer-group"></i>
        Plus {{tailCount}} more order(s) below the preview (not reordered).
    </p>
    {{/if}}

    <div class="imsc-dr-footer">
        <button type="button" data-action="confirmOrder" class="imsc-dr-confirm-btn">
            <i class="fa-solid fa-check"></i> Confirm Order
        </button>
        <button type="button" data-action="cancelOrder" class="imsc-dr-cancel-btn">
            <i class="fa-solid fa-times"></i> Cancel
        </button>
    </div>

</div>
