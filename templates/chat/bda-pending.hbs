{{!-- bda-pending.hbs  -  BDA awaiting-assessment + result chat card --}}
<div class="imsc-fire-result imsc-bda-pending">

    {{!-- ═ Header ═ --}}
    <div class="imsc-fire-header">
        <div class="imsc-bda-icon-wrap">
            <i class="fa-solid fa-satellite-dish"></i>
        </div>
        <div class="imsc-fire-title">
            <strong>{{localize "IMSC.BDA.Title"}}</strong>
            {{#if rolled}}
                {{#if success}}
                    <span class="imsc-fire-mode imsc-fire-success">{{localize "IMSC.BDA.Assessed"}}</span>
                {{else if hasFireResult}}
                    <span class="imsc-fire-mode imsc-fire-failure">{{localize "IMSC.BDA.LockLostSubtitle"}}</span>
                {{else}}
                    <span class="imsc-fire-mode imsc-fire-failure">{{localize "IMSC.BDA.FailedSubtitle"}}</span>
                {{/if}}
            {{else}}
                <span class="imsc-fire-mode">{{localize "IMSC.BDA.PendingSubtitle"}}</span>
            {{/if}}
        </div>
    </div>

    {{!-- ═ Target Row ═ --}}
    <div class="imsc-fire-target-row">
        <i class="fa-solid fa-crosshairs"></i>
        <span>{{targetName}}</span>
    </div>

    {{#if rolled}}
    {{!-- ═══ Post-Roll State ═══ --}}

    {{!-- SL / Outcome block --}}
    <div class="imsc-fire-roll">
        {{#if success}}
            <div class="imsc-fire-sl imsc-fire-success">{{signedSL}} SL</div>
            <div class="imsc-fire-outcome imsc-fire-success">{{outcome}}</div>
        {{else}}
            <div class="imsc-fire-sl imsc-fire-failure">{{signedSL}} SL</div>
            <div class="imsc-fire-outcome imsc-fire-failure">{{outcome}}</div>
        {{/if}}
    </div>

    {{!-- Correction block (success only) --}}
    {{#if success}}
    {{#if correctionChosen}}
    <div class="imsc-bda-correction-result">
        <i class="{{correctionIcon}}"></i>
        <div class="imsc-bda-correction-info">
            <span class="imsc-bda-correction-label">{{correctionLabel}}</span>
            <span class="imsc-bda-correction-desc">{{correctionDesc}}</span>
        </div>
    </div>
    {{else}}
    <div class="imsc-bda-pending-msg imsc-bda-pending-msg--subtle">
        <i class="fa-solid fa-arrow-right"></i>
        {{localize "IMSC.BDA.AwaitingCorrection"}}
    </div>
    {{/if}}
    {{/if}}

    {{!-- ═══ Embedded Attack Result ═══ --}}
    {{#if hasFireResult}}
    <div class="imsc-bda-fire-result-divider">
        <span>{{localize "IMSC.BDA.AttackResult"}}</span>
    </div>
    {{{fireResultHtml}}}
    {{else if rolled}}
    {{!-- BDA failed  -  no attack data available --}}
    <div class="imsc-bda-failed-notice">
        {{localize "IMSC.BDA.FailedNotice"}}
    </div>
    {{/if}}

    {{else}}
    {{!-- ═══ Pre-Roll State ═══ --}}
    <div class="imsc-bda-pending-msg">
        <i class="fa-regular fa-hourglass-half"></i>
        {{localize "IMSC.BDA.Pending"}}
    </div>
    {{#if weaponName}}
    <div class="imsc-bda-weapon-summary">
        {{#if weaponImg}}<img src="{{weaponImg}}" class="imsc-bda-weapon-icon">{{/if}}
        <span class="imsc-bda-weapon-name">{{weaponName}}</span>
        {{#if fireModeLabel}}<span class="imsc-bda-fire-mode">{{fireModeLabel}}</span>{{/if}}
    </div>
    {{/if}}
    <button class="imsc-bda-launch-btn" data-action="openBDAFromChat">
        <i class="fa-solid fa-dice"></i>
        {{localize "IMSC.BDA.LaunchAssessment"}}
    </button>
    {{/if}}

</div>
